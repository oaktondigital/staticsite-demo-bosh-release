#!/bin/bash -ex
WEBSITE_DIR=<%= p('website_docroot') %>
mkdir -p $(dirname $WEBSITE_DIR)
if [[ -d "$WEBSITE_DIR" ]]; then
  rm -rf $WEBSITE_DIR
  fi
cp -a /var/vcap/packages/website/git $WEBSITE_DIR
chown -R vcap:vcap $WEBSITE_DIR
